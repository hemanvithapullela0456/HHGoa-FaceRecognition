# Face detection + recognition
# OpenCV ships YuNet (detect) + SFace (recognize) as built-in ONNX models — no
# compiler needed, unlike insightface/dlib. Models are auto-downloaded on first run.
opencv-python>=4.9,<5
numpy>=1.26,<3

# Search + web  (Pinata / catbox / SerpApi are all plain HTTPS via requests)
google-search-results>=2.4.2   # SerpApi client
requests>=2.31,<3
beautifulsoup4>=4.12,<5
lxml>=5.0

# Blockchain
web3>=7.0,<8
eth-account>=0.13,<0.14
py-solc-x>=2.0            # only used if contracts/AttestationRegistry.json is absent

# CLI + config
typer>=0.15,<1
python-dotenv>=1.0,<2
rich>=13.7

# Dev
pytest>=8.0
